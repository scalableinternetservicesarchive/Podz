<% @favorites.each do |favorite| %>
  $('#<%= favorite.item_id %>').html(
      '<%= link_to image_tag("full_star.png"), favorite_path(item_id: favorite.item_id), remote: true, method: :delete %>'
  )
<% end %>
